export interface Experience {
  role: string
  company: string
  location: string
  period: string
}

export interface Skill {
  title: string
  description: string
  icon: string
}

export interface ContactLink {
  label: string
  url: string
  icon: string
}
